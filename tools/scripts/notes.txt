
DONE:

moved filesystem navigation helpers to common lib
removed temporary hardcoding of library paths in kernel.rs
renamed common::data -> common::state
renamed compile_helpers -> tool_helpers
renamed compile_settings -> tool_settings

(Make a commit here)

+ have compilation scripts run in threads. read the child process output in realtime.

(Actually just like make the game)
